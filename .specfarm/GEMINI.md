# SpecFarm Infrastructure — DO NOT MODIFY

> **This directory is managed by SpecFarm. AI coding agents must not add, modify, or delete files here.**

## For Gemini CLI

The `.specfarm/` directory is **SpecFarm-managed infrastructure**. It contains drift detection, rule enforcement, and CI tooling installed and versioned by SpecFarm.

### You MUST NOT:
- Add new source files, scripts, or configuration to `.specfarm/`
- Modify existing scripts, templates, or configuration in `.specfarm/`
- Create new subdirectories inside `.specfarm/`
- Delete or reorganize files within `.specfarm/`

### Legitimate changes to `.specfarm/` are made exclusively by:
```bash
.specfarm/bin/specfarm-install.sh --target <this-repo>
```

<!-- SpecFarm-managed: do not edit this file -->
